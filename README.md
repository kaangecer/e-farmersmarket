This repository contains the FoodLoop Flask web application.
To run the app locally, create and activate a virtual environment, install dependencies with pip install -r requirements.txt, set FLASK_APP=app.py, initialize the database (and any seed data), then start the development server using flask run and open the shown URL in your browser.
