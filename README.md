This repository contains the FoodLoop Flask web application.

To run the app locally:
1. create and activate a virtual environment,
2. install dependencies with pip install -r requirements.txt,
3. set FLASK_APP=app.py,
4. initialize the database (and any seed data),
5. then start the development server using flask run and open the shown URL in your browser.
